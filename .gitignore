# Dependencies
node_modules/

# Build output
# dist/ is NOT gitignored — commit dist/ to git and GitHub Pages serves it directly
# (only encrypted .enc files and static frontend are public, no secrets inside dist/)

# Raw vault (never commit your actual notes)
vault/

# Password file (contains secrets — reference passwords.md.example)
passwords.md

# Claude session / skill data (never commit)
.claude/

# OS files
.DS_Store
Thumbs.db

# Editor
.vscode/
*.swp
